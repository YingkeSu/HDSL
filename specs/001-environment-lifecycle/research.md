# 上游验证计划

状态：待执行。原 ChatGPT 对话只能提供调研线索，不能替代以下证据。

| 编号 | 必须回答的问题 | 证据/完成标准 |
| --- | --- | --- |
| R001 | DSH 官方仓库、发布来源和许可证是什么？ | 官方 URL、commit/tag、授权范围，下载验证方式 |
| R002 | 支持哪些 Node/OS/arch？ | 固定两个候选组合，CLI help 与安装日志 |
| R003 | 哪些参数决定 profile、配置、插件、会话和 home？ | 两环境标记实验，监测宿主默认目录是否被写入 |
| R004 | 如何启动 WebUI、检测就绪并停止进程树？ | 真实命令、loopback 绑定、端口冲突、超时、终止证据 |
| R005 | 插件/bundle 能否锁版本并隔离安装？ | 原生命令、依赖解析、安装脚本行为及现有限制 |
| R006 | 升级是否迁移可变数据？ | 备份、迁移、恢复实验；支持与拒绝边界 |

完成即停：记录可支持的最小版本矩阵和适配契约，或记录具体阻塞与缩小范围建议；不无限等待不存在的发布或 CI。禁止在未验证前编写“兼容所有版本”的实现。
