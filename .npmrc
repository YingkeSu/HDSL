# Lock the toolchain instead of floating to whatever a contributor has.
# engines are declared in package.json; engine-strict makes a mismatch a hard
# install failure rather than a warning.
engine-strict=true
# Write exact versions for anything added with `pnpm add`.
save-exact=true
